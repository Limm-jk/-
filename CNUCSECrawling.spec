# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['CNUCSECrawling.py'],
             pathex=['C:\\Users\\Free\\Desktop\\오늘의 크롤링~\\공지보기귀찮아'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='CNUCSECrawling',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=True , icon='comp_icon.ico')
